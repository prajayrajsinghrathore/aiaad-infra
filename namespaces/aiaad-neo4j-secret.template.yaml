apiVersion: v1
kind: Secret
metadata:
  name: aiaad-neo4j-credentials
  namespace: aiaad-platform
type: Opaque
# IMPORTANT: This is a template. Do NOT commit actual credentials to Git.
# Populate this secret in the cluster securely before running the smoke tests.
stringData:
  NEO4J_URI: "bolt+routing://neo4j-aura-placeholder:7687"
  NEO4J_USERNAME: "neo4j"
  NEO4J_PASSWORD: "CHANGE_ME"
