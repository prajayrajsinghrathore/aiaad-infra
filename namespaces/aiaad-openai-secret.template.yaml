apiVersion: v1
kind: Secret
metadata:
  name: openai-service-account
  namespace: aiaad-platform
type: Opaque
# IMPORTANT: This is a template. Do NOT commit actual credentials to Git.
# Mapped dynamically from Azure Key Vault secret "openai-service-account"
stringData:
  OPENAI_API_KEY: "CHANGE_ME"
