# environments/hackathon/environment.yaml
# Non-secret environment-wide configuration values

metadata:
  environment: hackathon
  project: aiaad

kubernetes:
  # The target context that the bootstrap script must use/verify.
  # Replace with the actual target context (e.g. brave-lion-admin or phoenix)
  targetContext: "brave-lion"

azure:
  # Subscription/Tenant details for workload identity correlation
  subscriptionId: "3e3fadd8-a910-430e-840b-79ff38a7eb49"
  tenantId: "821e7efe-4ccd-4c7f-a717-31bd85305b4e"
  resourceGroup: "rg-hackathon"
  keyVaultName: "kv-jbluejipmztn4"
  storageAccountName: "stjbluejipmztn4"
  workloadIdentityName: "id-workload-brave-lion"

externalServices:
  # Managed service endpoints (non-secret DNS / configuration paths)
  neo4jUri: "bolt+routing://neo4j-aura-placeholder:7687"
  azureAiFoundryEndpoint: "https://aiaad-foundry-placeholder.openai.azure.com/"
  sharePointTenantUrl: "https://placeholder.sharepoint.com"
